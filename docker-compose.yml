services:
  markdown-preview:
    build: .
    container_name: markdown-preview
    ports:
      - "3000:3000"
    volumes:
      - ./markdown:/app/markdown
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
